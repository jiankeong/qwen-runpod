diff --git a/README.md b/README.md
index e4d2a33..95ba27b 100644
--- a/README.md
+++ b/README.md
@@ -24,6 +24,8 @@ The Hub smoke test targets an RTX 4090, matching the working
 `qwen-image-2-1` configuration and avoiding the unavailable A40/A6000/L40 pool.
 Normal deployments keep `USE_MOCK_PIPELINE=0`, download Q4_K_M into the mounted
 Network Volume on first use, and reuse it on later workers with 16K context.
+The worker registers with the RunPod queue only after llama.cpp reports healthy,
+so cold-start download and model loading do not consume the job execution timer.
 
 ## 1. Build and push
 
diff --git a/ROLLBACK.sh b/ROLLBACK.sh
index 1be4c86..83b1849 100755
--- a/ROLLBACK.sh
+++ b/ROLLBACK.sh
@@ -2,6 +2,14 @@
 set -euo pipefail
 
 root="${1:-$(pwd)}"
+if [[ "${2:-}" == "--worker-readiness-only" ]]; then
+  for path in handler.py tests/test_handler.py README.md; do
+    git -C "$root" show "v1.0.11:$path" > "$root/$path"
+  done
+  printf 'ROLLBACK_OK: restored v1.0.11 early worker registration\n'
+  exit 0
+fi
+
 if [[ "${2:-}" == "--test-gpu-4090-only" ]]; then
   for path in .runpod/tests.json README.md tests/test_runpod_hub.py; do
     git -C "$root" show "v1.0.10:$path" > "$root/$path"
diff --git a/VERIFICATION.txt b/VERIFICATION.txt
index b676b35..fc62b90 100644
--- a/VERIFICATION.txt
+++ b/VERIFICATION.txt
@@ -1,34 +1,34 @@
-RunPod v1.0.11 Hub smoke-test GPU verification
+RunPod v1.0.12 cold-start execution-timeout verification
 
 Changed branch/field
-- Branch: RunPod Hub validation capacity selection
-- Fields: .runpod/tests.json config.gpuTypeId changes from NVIDIA A40 to NVIDIA GeForce RTX 4090; CUDA list remains 12.8-13.3
+- Branch: normal Serverless worker initialization and queue registration
+- Fields: handler.py moves wait_until_ready(STARTUP_TIMEOUT) from handler(job) to main() before runpod.serverless.start()
 
 Artifacts
-- MODIFIED_FILE: /Users/jk/PhpstormProjects/qwen-runpod/.runpod/tests.json
+- MODIFIED_FILE: /Users/jk/PhpstormProjects/qwen-runpod/handler.py
 - DIFF_FILE: /Users/jk/PhpstormProjects/qwen-runpod/changes.diff
 - VERIFICATION: /Users/jk/PhpstormProjects/qwen-runpod/VERIFICATION.txt
 - ROLLBACK: /Users/jk/PhpstormProjects/qwen-runpod/ROLLBACK.sh
 
 Hashes
-- Original .runpod/tests.json SHA-256: bceceaf7a2a55f6f7524db661de5be7d085e9666266aa677b6af3e34ccfcdd91
-- Modified .runpod/tests.json SHA-256: 7e92ba9c7b2fb0083056064c97d77ccb86376266a43717ba4df46485d8054043
+- Original handler.py SHA-256: 523739c0adb1ac535962570b0bdb7319afdc34edb25ad928dc6899b3c3f691c7
+- Modified handler.py SHA-256: 7666569e571896a5e790876e75877de28cc1c54e4f485f6c792bf7b05a155dfa
 
 BASELINE
 Command: python3 -m unittest discover -s tests -v 2>&1; rc=$?; printf 'EXIT_STATUS=%s\n' "$rc"; exit "$rc"
-Input: v1.0.10 A40 Hub smoke-test configuration
-Literal output/result: Ran 11 tests in 0.006s; OK; EXIT_STATUS=0
+Input: v1.0.11 early queue registration with readiness wait inside each job
+Literal output/result: Ran 11 tests in 0.008s; OK; EXIT_STATUS=0
 Exit status: 0
 
 MODIFIED
-Command: python3 -m json.tool .runpod/tests.json >/dev/null && python3 -m unittest discover -s tests -v 2>&1 && python3 -c 'import json; c=json.load(open(".runpod/tests.json")); print("MODIFIED_GPU="+c["config"]["gpuTypeId"]); print("MODIFIED_CUDA="+",".join(c["config"]["allowedCudaVersions"]))'; rc=$?; printf 'EXIT_STATUS=%s\n' "$rc"; exit "$rc"
-Input: RTX 4090 Hub smoke-test configuration copied from qwen-image-2-1
-Literal output/result: Ran 11 tests in 0.007s; OK; MODIFIED_GPU=NVIDIA GeForce RTX 4090; MODIFIED_CUDA=12.8,12.9,13.0,13.1,13.2,13.3; EXIT_STATUS=0
+Command: python3 -m unittest discover -s tests -v 2>&1 && python3 -c 'from pathlib import Path; h=Path("handler.py").read_text(); body=h[h.index("def handler"):h.index("def main")]; main=h[h.index("server = subprocess.Popen"):]; print("MODIFIED_HANDLER_WAITS="+str("wait_until_ready" in body)); print("MODIFIED_MAIN_WAITS_BEFORE_WORKER="+str(main.index("wait_until_ready") < main.index("runpod.serverless.start")))'; rc=$?; printf 'EXIT_STATUS=%s\n' "$rc"; exit "$rc"
+Input: v1.0.12 model readiness before normal RunPod worker registration
+Literal output/result: Ran 12 tests in 0.013s; OK; MODIFIED_HANDLER_WAITS=False; MODIFIED_MAIN_WAITS_BEFORE_WORKER=True; EXIT_STATUS=0
 Exit status: 0
 
 ROLLBACK
-Command: tmp=$(mktemp -d /tmp/qwen-runpod-v111-rollback.XXXXXX) && cp -R . "$tmp/project" && ./ROLLBACK.sh "$tmp/project" --test-gpu-4090-only && python3 -c 'import json,sys; from pathlib import Path; root=Path(sys.argv[1]); c=json.loads((root/".runpod/tests.json").read_text()); print("ROLLBACK_GPU="+c["config"]["gpuTypeId"]); print("ROLLBACK_README_A40="+str("targets an A40" in (root/"README.md").read_text())); assert c["config"]["gpuTypeId"]=="NVIDIA A40"' "$tmp/project"; rc=$?; printf 'ROLLBACK_COPY=%s\nEXIT_STATUS=%s\n' "$tmp/project" "$rc"; exit "$rc"
-Input: separate copy of v1.0.11 workspace
-Literal output/result: ROLLBACK_OK: restored v1.0.10 A40 Hub test GPU; ROLLBACK_GPU=NVIDIA A40; ROLLBACK_README_A40=True; ROLLBACK_COPY=/tmp/qwen-runpod-v111-rollback.wMScon/project; EXIT_STATUS=0
+Command: tmp=$(mktemp -d /tmp/qwen-runpod-v112-rollback.XXXXXX) && cp -R . "$tmp/project" && ./ROLLBACK.sh "$tmp/project" --worker-readiness-only && python3 -c 'import sys; from pathlib import Path; root=Path(sys.argv[1]); h=(root/"handler.py").read_text(); body=h[h.index("def handler"):h.index("def main")]; main=h[h.index("server = subprocess.Popen"):]; print("ROLLBACK_HANDLER_WAITS="+str("wait_until_ready" in body)); print("ROLLBACK_MAIN_WAITS_BEFORE_WORKER="+str("wait_until_ready" in main and main.index("wait_until_ready") < main.index("runpod.serverless.start"))); assert "wait_until_ready" in body' "$tmp/project"; rc=$?; printf 'ROLLBACK_COPY=%s\nEXIT_STATUS=%s\n' "$tmp/project" "$rc"; exit "$rc"
+Input: separate copy of v1.0.12 workspace
+Literal output/result: ROLLBACK_OK: restored v1.0.11 early worker registration; ROLLBACK_HANDLER_WAITS=True; ROLLBACK_MAIN_WAITS_BEFORE_WORKER=False; ROLLBACK_COPY=/tmp/qwen-runpod-v112-rollback.QLKME4/project; EXIT_STATUS=0
 Exit status: 0
-Restored behavior/status: rollback copy targets A40 again; live MODIFIED_FILE targets RTX 4090 and retains the same CUDA compatibility list.
+Restored behavior/status: rollback copy again claims the queue job before model readiness; live MODIFIED_FILE waits for llama.cpp health before accepting RunPod work.
diff --git a/handler.py b/handler.py
index b2a77cb..6fb976e 100644
--- a/handler.py
+++ b/handler.py
@@ -94,9 +94,6 @@ def handler(job: dict[str, Any]) -> dict[str, Any]:
     if payload.get("stream"):
         raise ValueError("stream=true is unsupported by RunPod queue jobs; use stream=false")
 
-    # Register the RunPod worker before the large GGUF finishes downloading.
-    # The first accepted job waits here while llama.cpp becomes ready.
-    wait_until_ready(int(os.getenv("STARTUP_TIMEOUT", "1800")))
     payload.setdefault("model", os.getenv("MODEL", "unsloth/Qwen3.8-27B-GGUF:UD-Q4_K_M"))
     return forward(route, payload)
 
@@ -113,6 +110,10 @@ def main() -> None:
     os.environ["HF_HOME"] = cache_dir
     server = subprocess.Popen(build_server_command())
     try:
+        # Finish the cold start before registering the queue worker. Otherwise,
+        # RunPod counts model loading against the job execution timeout and can
+        # reject an already-generated result after the 600-second deadline.
+        wait_until_ready(int(os.getenv("STARTUP_TIMEOUT", "1800")))
         import runpod
 
         runpod.serverless.start({"handler": handler})
diff --git a/tests/test_handler.py b/tests/test_handler.py
index a52e32f..ad656db 100644
--- a/tests/test_handler.py
+++ b/tests/test_handler.py
@@ -19,7 +19,7 @@ class HandlerTests(unittest.TestCase):
         self.assertEqual(route, "/v1/chat/completions")
         self.assertIn("model", payload)
         self.assertEqual(result["choices"][0]["message"]["content"], "ok")
-        self.ready.assert_called_once_with(1800)
+        self.ready.assert_not_called()
 
     @patch("handler.forward")
     def test_completion_route(self, forward):
@@ -63,6 +63,22 @@ class HandlerTests(unittest.TestCase):
         popen.assert_not_called()
         fake_runpod.serverless.start.assert_called_once()
 
+    def test_normal_main_waits_for_model_before_registering_worker(self):
+        fake_runpod = MagicMock()
+        server = MagicMock()
+        events = []
+        fake_runpod.serverless.start.side_effect = lambda config: events.append("worker_registered")
+        with (
+            patch.dict(os.environ, {"USE_MOCK_PIPELINE": "0", "STARTUP_TIMEOUT": "1800"}, clear=True),
+            patch.dict("sys.modules", {"runpod": fake_runpod}),
+            patch("handler.os.makedirs"),
+            patch("handler.subprocess.Popen", return_value=server),
+            patch("handler.wait_until_ready", side_effect=lambda timeout: events.append(f"ready:{timeout}")),
+        ):
+            handler.main()
+        self.assertEqual(events, ["ready:1800", "worker_registered"])
+        server.terminate.assert_called_once_with()
+
 
 if __name__ == "__main__":
     unittest.main()
